# 组合纹理缓存必须持久化且可由玩家控制

组合纹理除本轮运行时复用外，还必须跨游戏重启持久化，并在设置中提供玩家可见、默认关闭的开关。开启时将本轮已有的组合 Blob 在后台补写入持久化缓存；持久化项保存编码图片 Blob 与签名，命中后只需解码为本轮可直接显示的合成立绘，不再加载全部部件并重新合成。关闭开关时立即删除所有持久化组合缓存；设置页另提供独立清理操作。配额或编码失败时，游戏继续使用本轮缓存并提示玩家，同时关闭持久化开关以避免重复失败；本轮合成立绘的回收策略后置处理。
