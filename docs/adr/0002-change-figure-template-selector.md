# changeFigure 使用角色模板选择符

组合立绘通过 `changeFigure:<角色模板>/<组合列表>` 声明，例如 `changeFigure:hana/summer_uniform_smile,cry,sad -left;`。斜杠前选择角色模板，斜杠后在该模板中解析逗号分隔的部件和组合预设；这避免了跨角色的通用名称冲突，同时保留 `changeFigure` 既有的位置和演出参数。
