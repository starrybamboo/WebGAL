# 持久化组合纹理暂用浏览器原生 WebP

首期持久化组合纹理使用 `canvas.toBlob('image/webp', 1)` 生成 WebP Blob，不引入 libwebp WASM 或其他编码依赖。浏览器没有可移植的无损、精确保真控制项，质量参数 `1` 不构成无损保证；因此这一限制必须在 RFC 和玩家持久化行为中明确标注。未来采用可控编码器时，自动失效并重建旧原生编码缓存，不要求玩家手动清理。
